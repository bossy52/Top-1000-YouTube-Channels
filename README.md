# 🪙Top 1000 YouTube Channels Interactive Dashboard

*จัดทำโดย:* [ใส่ชื่อ-นามสกุล และรหัสนักศึกษาของคุณ]
*ลิงก์สำหรับเข้าใช้งาน Dashboard:* [ใส่ลิงก์ Streamlit Cloud เช่น https://your-app.streamlit.app]
*ชุดข้อมูลที่ใช้ (Dataset):* Crypto Data จาก Kaggle (ไฟล์ .CSV)

---

1. วัตถุประสงค์และเครื่องมือที่ใช้ในการพัฒนา

โปรเจกต์นี้เป็นการสร้าง Interactive Dashboard เพื่อแสดงผลสถิติและวิเคราะห์ข้อมูลของช่อง YouTube ที่มีผู้ติดตามสูงที่สุดในโลก โดยสามารถเปิดใช้งานได้ผ่าน Web Browser

Dashboard นี้ช่วยให้ผู้ใช้สามารถสำรวจข้อมูล เช่น

จำนวนผู้ติดตาม (Subscribers)

ยอดการรับชมวิดีโอ (Views)

จำนวนวิดีโอของแต่ละช่อง

หมวดหมู่ของช่อง YouTube

เครื่องมือที่ใช้:

ภาษาที่ใช้: Python

Streamlit: สำหรับสร้างหน้าเว็บ Dashboard และระบบ Interactive

Pandas: สำหรับอ่านและจัดการข้อมูล (Dataframe)

Plotly: สำหรับสร้างกราฟ Interactive และแสดงตัวเลขบนกราฟ (Chart Data Labels)

📖 2. คู่มือการใช้งาน Dashboard (User Guide)
ขั้นตอนที่ 1: การอัปโหลดข้อมูล

เมื่อเปิดลิงก์ Dashboard ให้ไปที่แถบเมนูด้านซ้ายมือ (Sidebar) แล้วกดปุ่ม Browse files จากนั้นเลือกไฟล์ Dataset นามสกุล .CSV ของข้อมูล Top 1000 YouTube Channels ที่ดาวน์โหลดมาจาก Kaggle

(เว้นบรรทัดไว้สำหรับใส่ภาพหน้าจอตอนอัปโหลดไฟล์)

ขั้นตอนที่ 2: การกรองข้อมูลแบบเฉพาะเจาะจง

หลังจากอัปโหลดไฟล์สำเร็จ แถบด้านซ้ายมือจะแสดงเมนู Filter ข้อมูล ผู้ใช้สามารถกรองข้อมูลได้ เช่น

เลือก Category (หมวดหมู่ช่อง YouTube) ที่ต้องการดู

เลื่อน Rank Slider (อันดับของช่อง) เพื่อกำหนดช่วงของอันดับช่องที่ต้องการวิเคราะห์

เมื่อมีการเลือก Filter ระบบจะอัปเดตกราฟและข้อมูลแบบอัตโนมัติ

(เว้นบรรทัดไว้สำหรับใส่ภาพหน้าจอส่วน Filter)

ขั้นตอนที่ 3: การดูสถิติและกราฟแสดงผล

พื้นที่หลักของ Dashboard จะแสดงข้อมูลสำคัญ ได้แก่

จำนวนช่องทั้งหมด (Total Channels)

จำนวนผู้ติดตามรวมทั้งหมด (Total Subscribers)

ยอดวิวรวมทั้งหมด (Total Views)

รวมถึงกราฟ Interactive เช่น

Top 10 ช่อง YouTube ที่มี Subscribers มากที่สุด

กราฟสัดส่วนหมวดหมู่ช่อง (Category Distribution)

กราฟความสัมพันธ์ระหว่าง Subscribers และ Video Views

กราฟทั้งหมดมีการตั้งค่า Chart Data Labels เพื่อแสดงตัวเลขบนกราฟ ทำให้สามารถอ่านค่าได้ง่ายและชัดเจน

(เว้นบรรทัดไว้สำหรับใส่ภาพหน้าจอกราฟ)

ขั้นตอนที่ 4: การดูรายละเอียดข้อมูลทั้งหมด

เมื่อเลื่อนลงมาด้านล่างสุดของ Dashboard จะพบกับตาราง Full Dataset ซึ่งแสดงข้อมูลทั้งหมดจากไฟล์ CSV ที่อัปโหลดเข้ามา
